<%- include('../partials/header.ejs', { title: 'Brand Carousel Details' }) %>

<nav class="flex flex-wrap gap-3 mb-8 p-4 bg-white rounded-xl shadow-sm border border-gray-100 items-center">
  <a href="/admin/v1/staff/dashboard" class="flex items-center gap-2 px-4 py-2 rounded-lg bg-blue-50 text-blue-700 hover:bg-blue-100 transition">
    <i class="fa fa-home"></i> Dashboard
  </a>
  <a href="<%= baseUrl %>" class="flex items-center gap-2 px-4 py-2 rounded-lg bg-gray-50 text-gray-700 hover:bg-gray-100 transition">
    <i class="fa fa-tags"></i> Brand Carousel
  </a>
  <a href="<%= baseUrl %>/<%= item._id %>/edit" class="flex items-center gap-2 px-4 py-2 rounded-lg bg-yellow-50 text-yellow-700 hover:bg-yellow-100 transition">
    <i class="fa fa-edit"></i> Edit Item
  </a>
</nav>

<main class="container mx-auto max-w-2xl px-4">
  <!-- Flash Messages -->
  <% if (error && error.length) { %>
    <div class="mb-6 p-4 rounded-lg border-l-4 border-red-400 bg-red-50 text-red-800 shadow-sm">
      <% error.forEach(msg => { %>
        <div class="flex items-center gap-2">
          <i class="fa fa-exclamation-circle"></i>
          <%= msg %>
        </div>
      <% }) %>
    </div>
  <% } %>
  <% if (success && success.length) { %>
    <div class="mb-6 p-4 rounded-lg border-l-4 border-green-400 bg-green-50 text-green-800 shadow-sm">
      <% success.forEach(msg => { %>
        <div class="flex items-center gap-2">
          <i class="fa fa-check-circle"></i>
          <%= msg %>
        </div>
      <% }) %>
    </div>
  <% } %>

  <div class="bg-white rounded-xl shadow-md p-8 mb-10 border border-gray-100">
    <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center mb-8 gap-4">
      <h1 class="text-2xl font-bold text-gray-800">Brand Carousel Details</h1>
      <a href="<%= baseUrl %>" class="text-sm text-blue-600 hover:text-blue-700 flex items-center transition">
        <span class="mr-1">←</span> Back to List
      </a>
    </div>
    
    <div class="space-y-6">
      <!-- Brand Logo -->
      <% if (item.brand.logo) { %>
        <div class="text-center">
          <img src="/uploads/<%= item.brand.logo %>" alt="<%= item.brand.name %>" class="max-w-full h-auto max-h-48 rounded-lg shadow-lg border mx-auto object-contain">
        </div>
      <% } %>
      
      <!-- Details Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="bg-gray-50 p-4 rounded-lg">
          <label class="block text-sm font-semibold text-gray-600 mb-2">Brand Name</label>
          <p class="text-lg text-gray-900"><%= item.brand.name %></p>
        </div>
        
        <div class="bg-gray-50 p-4 rounded-lg">
          <label class="block text-sm font-semibold text-gray-600 mb-2">Display Order</label>
          <span class="inline-flex items-center px-3 py-1 rounded-md text-sm font-medium bg-blue-100 text-blue-800">
            <%= item.order %>
          </span>
        </div>
        
        <div class="bg-gray-50 p-4 rounded-lg">
          <label class="block text-sm font-semibold text-gray-600 mb-2">Status</label>
          <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-semibold
            <%= item.status === 'active' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-700' %>">
            <i class="fa fa-circle text-xs mr-2 <%= item.status === 'active' ? 'text-green-500' : 'text-gray-500' %>"></i>
            <%= item.status || 'active' %>
          </span>
        </div>
        
        <div class="bg-gray-50 p-4 rounded-lg">
          <label class="block text-sm font-semibold text-gray-600 mb-2">Added By</label>
          <% if (item.admin) { %>
            <p class="text-gray-900"><%= item.admin.username %></p>
          <% } else { %>
            <p class="text-gray-500">Admin not found</p>
          <% } %>
        </div>
      </div>
      
      <!-- Metadata -->
      <div class="border-t pt-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm text-gray-600">
          <div>
            <span class="font-semibold">Added:</span> 
            <%= new Date(item.createdAt).toLocaleDateString() %> at <%= new Date(item.createdAt).toLocaleTimeString() %>
          </div>
          <div>
            <span class="font-semibold">Last Updated:</span> 
            <%= new Date(item.updatedAt).toLocaleDateString() %> at <%= new Date(item.updatedAt).toLocaleTimeString() %>
          </div>
        </div>
      </div>
      
      <!-- Actions -->
      <div class="flex flex-col sm:flex-row gap-3 sm:justify-end pt-6 border-t">
        <a href="<%= baseUrl %>/<%= item._id %>/edit" class="px-4 py-3 rounded-lg bg-yellow-100 text-yellow-700 hover:bg-yellow-200 transition flex items-center justify-center gap-2">
          <i class="fa fa-edit"></i> Edit Item
        </a>
        <form action="<%= baseUrl %>/<%= item._id %>?_method=DELETE" method="POST" class="inline">
          <button type="submit" class="w-full px-4 py-3 rounded-lg bg-red-100 text-red-600 hover:bg-red-200 transition flex items-center justify-center gap-2" onclick="return confirm('Remove this brand from carousel? This action cannot be undone.')">
            <i class="fa fa-trash"></i> Remove from Carousel
          </button>
        </form>
      </div>
    </div>
  </div>
</main>

<%- include('../partials/footer.ejs') %>